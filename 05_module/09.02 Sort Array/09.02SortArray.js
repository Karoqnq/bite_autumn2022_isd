var array = ["Milk", "Bread", "Juice", "Eggs", "Butter", "Salt", "Oranges", "Apples"];

function sortArray() {
    // TODO


for(var i = 0; i < array.length; i++ ){

    array.sort();
    array.reverse();
    console.log(array[i])
}
}
